# Static frontend server with SPA fallback: unknown paths (Angular routes on
# refresh/deep link) serve index.html. Mounted over the nginx default config.
server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
